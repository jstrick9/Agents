resources:
  pipelines:
    onr_medallion:
      name: onr-itss-pipeline-${bundle.target}
      serverless: true
      catalog: ${var.catalog}
      target: ${var.schema}
      continuous: false
      configuration:
        onr.catalog: ${var.catalog}
        onr.schema: ${var.schema}
      libraries:
        - notebook:
            path: src/pipelines/medallion.py
